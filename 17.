/*Input a  number. 
check it for positive negative or zero using
only simple if statements
*/
#include<stdio.h>
#include<math.h>
int main()
{
    int number;
    printf("Enter a number to check for positive,negative,zero:");
    scanf("%d",&number);
    if(number>0)
        printf("%d is Positive",number);
    if(number<0)
        printf("%d is Negative",number);
    if(number==0)
        printf("Number is Zero");
    
	return 0;
}
